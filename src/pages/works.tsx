import { Text } from '@chakra-ui/react';
import Layout from 'components/layout';
import React from 'react';

const WorksPage = () => {
  return (
    <Layout>
      <Text> works portfolio </Text>
    </Layout>
  );
};

export default WorksPage;
